# Servidor
PORT=3000

# Banco de dados PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=nossavoz
DB_USER=postgres
DB_PASSWORD=sua_senha_aqui

# JWT
JWT_SECRET=troque_por_um_segredo_longo_e_aleatorio
JWT_EXPIRES_IN=8h

# Criptografia do conteúdo das solicitações (AES-256)
# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CRIPTO_CHAVE=troque_por_64_chars_hex_aqui

# Ambiente
NODE_ENV=development
